import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gohan-treinamentos-ionic-app',
  webDir: 'dist'
};

export default config;
